#include <iostream>

#include "test-icollection.h"
#include "../dynamic-array.h"
#include "icollection.h"

void test() {

    /*int array[10];
    for(int i=0; i < 10; i++)
        array[i] = i;

    DynamicArray<int> da(array, 10);

    char name[20] = "amogus";

    ICollection<DynamicArray<int>> ic(da, name);

    //DynamicArray<int> a = ic.Get(name);

    for(int i=0; i < 10; i++)
        std::cout<<a.Get(i);
    for(int i=0; i < 10; i++)
        std::cout<<da.Get(i);*/

}
